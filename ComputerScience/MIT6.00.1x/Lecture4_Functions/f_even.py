# Create a function which returns true if the x passed as argument is even, an false otherwise
def even(x):
    """
    Input: x: a positive int
    Returns: True if x is even, otherwise False
    """
    return (x % 2) == 0
